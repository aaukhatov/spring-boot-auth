language: java
jdk: oraclejdk8

sudo: required

services:
- docker

before_script:
- docker login -u $DOCKER_USER -p $DOCKER_PASSWORD

script:
 - ./gradlew clean pushImage

notifications:
  email: false